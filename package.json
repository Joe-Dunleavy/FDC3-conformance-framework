{
  "private": true,
  "workspaces": [
    "tests",
    "app",
    "mock"
  ],
  "scripts": {
    "build": "yarn workspaces run build && yarn run build:webpack",
    "build:webpack": "webpack build",
    "bra": "yarn build; cd app; yarn build; yarn start"
  },
  "devDependencies": {
    "ts-loader": "^9.4.1",
    "typescript": "^4.9.3",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.0"
  }
}
